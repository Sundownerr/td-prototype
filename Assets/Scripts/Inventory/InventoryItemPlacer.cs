using System.Collections.Generic;
using UnityEngine;
using System.Linq;
using Satisfy.Attributes;

namespace TestTD.UI
{
    public class InventoryItemPlacer : MonoBehaviour
    {
        // public void PlaceItem(InventoryItemUI item, InventoryCellUI closestCell)
        // {
        //     var placePos = closestCell.transform.position;
        //
        //     // item.SetPosition(placePos);
        //     item.SetPlaceState(InventoryItemUI.PlaceState.Placed);
        //     item.SetUsedCell(closestCell);
        // }
    }
}